# RPG AI — текстовая ролевая игра с поддержкой предметов, боя и исследования

## Описание

**RPG AI** — это консольная текстовая ролевая игра на Python, в которой игрок может:

- **Исследовать мир**: находить предметы, встречать монстров и события.
- **Сражаться**: вступать в бой с врагами, использовать оружие и зелья.
- **Управлять инвентарём**: подбирать предметы, экипировать броню и оружие.
- **Играть за разные классы**: воина, мага или лучника — каждый со своими уникальными характеристиками.
- **Интегрироваться с ИИ**: через отдельный модуль `script.py` поддерживается взаимодействие с GigaChat (в будущем — для генерации событий, диалогов или описаний).

Игра построена на принципах ООП и легко расширяется для добавления новых предметов, классов или типов взаимодействий.

---

## Основные компоненты

### Персонажи (классы)
- **`Mob`** — базовый класс для всех живых существ (игрок и монстры).
- **`Warrior`**, **`Wizard`**, **`Archer`** — наследуют от `Mob`, имеют уникальные параметры:
  - Воин: сила, прочность, дух.
  - Маг: мудрость, концентрация, интуиция.
  - Лучник: ловкость, зрение, выносливость.

### Предметы
- **`Item`** — абстрактный базовый предмет.
- **`HealthBottle`** — восстанавливает здоровье.
- **`Armor`** — повышает защиту при экипировке. Особенности снятия зависят от класса персонажа.
- **`Weapon`** — увеличивает урон.

> ⚠️ **Примечание**: в текущей реализации есть баг в методах `use`/`unuse` у `Armor` и `Weapon` (например, проверка `equipped_armor` вместо `equipped_weapon` при экипировке оружия). Планируется исправление.

### Системы
- **`Inventory`** — управление предметами игрока.
- **`World`** — хранит состояние игры: мобов, предметы, события.
- **`Situation`** — абстрактный базовый класс для игровых сцен.
  - **`Exploration`** — режим исследования: осмотр, подбор предметов, начало боя.
  - **`Combat`** — режим боя: атака, побег, использование зелий.

### Утилиты
- **`safe_input`**, **`closest_match`** — защита от опечаток и некорректного ввода.
- **`show_stats`** — красивый вывод статистики игрока и врага.

---

## Запуск игры

1. Убедитесь, что установлены зависимости:
   ```bash
   pip install -r requirements.txt
   ```

   Пример `requirements.txt`:
   ```
   python-dotenv
   httpx
   gigachat  # (если используется GigaChat API)
   ```

2. Для работы с ИИ (опционально) создайте файл `.env` в корне проекта:
   ```env
   GIGACHAT_CREDS=your_base64_encoded_credentials
   ```

3. Запустите основной игровой цикл (в отдельном `main.py`):
   ```python
   from rpg_ai import Warrior, World, Exploration

   player = Warrior(
       name="Герой",
       hp=100,
       defence=10,
       damage=20,
       luck=70,
       stats=[15, 12, 10],  # сила, прочность, дух
       max_weight=50,
       max_health=100
   )

   world = World(
       player=player,
       combat_mobs=[],
       exploration_mobs=[],
       trade_mobs=[],
       exploration_items=[],
       events=[]
   )

   situation = world.generate_situation(Exploration)
   situation.process_input()
   ```

> 🔧 **Важно**: текущий код не содержит `main.py` — вы должны создать точку входа самостоятельно.

---

## Команды в игре

### В режиме исследования (`Exploration`)
- `/инвентарь` — показать инвентарь.
- `/осмотреться` — увидеть окружение и статус.
- `/взять --<предмет>` — подобрать предмет.
- `/использовать --<предмет>` — использовать предмет из инвентаря.
- `/атаковать --<враг>` — начать бой.

### В бою (`Combat`)
- `/атаковать` — атаковать врага.
- `/бежать` — попытаться сбежать (зависит от удачи).
- `/использовать --<зелье>` — применить зелье.
- `/инвентарь`, `/осмотреть` — просмотр данных.
- `/выйти из боя` — после победы.

> 📝 Все команды поддерживают автоисправление опечаток!

---

## Интеграция с ИИ (GigaChat)

Модуль `script.py` содержит класс `ResilientGiga`, который:

- Безопасно подключается к API GigaChat.
- Автоматически обновляет токен при 401-ошибке.
- Может использоваться для генерации:
  - описаний локаций,
  - диалогов с торговцами,
  - уникальных событий,
  - имен монстров или предметов.

Пример:
```python
g = ResilientGiga()
desc = g.chat("Опиши таинственную пещеру, полную артефактов.")
print(desc.choices[0].message.content)
```

---

## Планы на будущее

- [ ] Исправить баги в логике экипировки (`Armor`/`Weapon`).
- [ ] Добавить торговлю с NPC.
- [ ] Генерировать события и предметы через ИИ.
- [ ] Реализовать сохранение и загрузку игры.
- [ ] Добавить уровни и прокачку.

---

## Автор

Этот проект разработан как основа для экспериментов с игровой механикой и ИИ-ассистентами в RPG-контексте. Идеально подходит для обучения ООП, обработке пользовательского ввода и интеграции с внешними API.

> 🎮 Удачи в приключениях!
